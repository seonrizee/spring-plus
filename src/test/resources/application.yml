spring:
  datasource:
    url: jdbc:mysql://localhost:3307/nbc_expert_db_test?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&rewriteBatchedStatements=true&useServerPrepStmts=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
#        show_sql: true
#        format_sql: true
#        use_sql_comments: true


jwt:
  secret:
    key: bXktc2VjcmV0LWtleS1mb3ItdGVzdC1lbnZpcm9ubWVudC13aGljaC1pcy1sb25nLWVub3VnaA==

cloud:
  aws:
    region:
      static: ${AWS_REGION}
    credentials:
      access-key: ${AWS_ACCESS_KEY_ID}
      secret-key: ${AWS_SECRET_ACCESS_KEY}
    s3:
      bucket: ${AWS_S3_BUCKET_NAME}
